<!DOCTYPE html>
<html lang="en" data-theme="coffee">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= project.title %> - Climate Solutions</title>
    <link href="/css/main.css" rel="stylesheet" />
  </head>
  <body>
    <%- include('partials/navbar', { page: '' }) %>

    <div class="container mx-auto p-6">
      
      <div class="hero bg-base-200 rounded-lg p-6 my-4">
        <div class="hero-content text-center">
          <div class="max-w-md">
            <h1 class="text-3xl font-bold"><%= project.title %></h1>
            <p class="py-2 text-sm">Below, you will find detailed information about this project.</p>
          </div>
        </div>
      </div>

      
      <div class="max-w-3xl mx-auto bg-base-100 p-6 rounded-lg shadow">
        <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="w-full rounded mb-4" />

        <h2 class="text-xl font-semibold mb-2">Introduction</h2>
        <p class="mb-4"><%= project.intro_short %></p>

        <h2 class="text-xl font-semibold mb-2">Impact</h2>
        <p class="mb-4"><%= project.impact %></p>

        <a href="<%= project.original_source_url %>" target="_blank" class="btn btn-primary mb-4">
          More Info
        </a>

        <div id="quote" class="italic text-sm mb-4"></div>

        <a href="#" onclick="history.back(); return false;" class="btn btn-outline">Back</a>
      </div>
    </div>

    <script>
      document.addEventListener("DOMContentLoaded", () => {
        fetch("https://dummyjson.com/quotes/random")
          .then(res => res.json())
          .then(data => {
            document.getElementById("quote").innerText = `"${data.quote}" — ${data.author}`;
          })
          .catch(err => console.error(err));
      });
    </script>
  </body>
</html>
